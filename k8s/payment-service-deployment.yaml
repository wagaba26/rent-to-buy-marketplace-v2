apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: rent-to-own
  labels:
    app: payment-service
    tier: backend
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
        tier: backend
    spec:
      containers:
      - name: payment-service
        image: rent-to-own/payment-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3003
          name: http
        env:
        - name: PORT
          value: "3003"
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: NODE_ENV
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: PAYMENT_DB_HOST
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: DB_PORT
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: PAYMENT_DB_NAME
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: PAYMENT_DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: PAYMENT_DB_PASSWORD
        - name: RABBITMQ_URL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: RABBITMQ_URL
        - name: ENCRYPTION_KEY
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: ENCRYPTION_KEY
        - name: MOBILE_MONEY_PROVIDER
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: MOBILE_MONEY_PROVIDER
        - name: MOBILE_MONEY_API_KEY
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: MOBILE_MONEY_API_KEY
        - name: MOBILE_MONEY_API_SECRET
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: MOBILE_MONEY_API_SECRET
        - name: MOBILE_MONEY_CALLBACK_URL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: MOBILE_MONEY_CALLBACK_URL
        - name: MOBILE_MONEY_ENV
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: MOBILE_MONEY_ENV
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: rent-to-own
  labels:
    app: payment-service
spec:
  type: ClusterIP
  ports:
  - port: 3003
    targetPort: 3003
    protocol: TCP
    name: http
  selector:
    app: payment-service

